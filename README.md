# ☀️ Solar Panel Optimization & Dust Detection System
# نظام تحسين الطاقة الشمسية وكشف الغبار

## 📝 وصف المشروع | Project Description
هذا المشروع عبارة عن نظام ذكي يعتمد على **التعلم الآلي (Machine Learning)** لتحسين كفاءة ألواح الطاقة الشمسية.
يقوم النظام بتحليل بيانات الطقس (الإشعاع الشمسي ودرجة الحرارة) للتنبؤ بكمية الطاقة التي *يجب* أن تنتجها الألواح في الوضع المثالي. بمقارنة هذا التوقع مع الإنتاج الفعلي، يمكن للنظام اكتشاف ما إذا كان هناك **غبار متراكم** أو خلل فني يستدعي الصيانة.

This project is an AI-powered system designed to optimize solar panel efficiency. By analyzing weather data (Irradiation & Temperature), it predicts the *ideal* power output. Comparing this with actual output allows the system to detect **dust accumulation** or technical faults.

---

## ✨ المميزات | Features
1.  **AI Power Prediction**: تنبؤ دقيق بالطاقة المتوقعة باستخدام خوارزمية **XGBoost** (دقة 98.6%).
2.  **Dust Detection Logic**: خوارزمية ذكية لاكتشاف الغبار بناءً على الفاقد في الطاقة وحالة الطقس.
3.  **Smart Dashboard**: لوحة تحكم عصرية (Web Interface) لمراقبة النظام وإدخال البيانات.
4.  **Arabic Interface**: واجهة مستخدم عربية سهلة الاستخدام ومصممة بتقنية Glassmorphism.

---

## 🛠️ التقنيات المستخدمة | Technologies
*   **Python 3.x**: لغة البرمجة الأساسية.
*   **Flask**: إطار عمل الويب (Backend).
*   **XGBoost**: الموديل المستخدم للتنبؤ (Machine Learning).
*   **Pandas & NumPy**: لمعالجة وتحليل البيانات.
*   **HTML5/CSS3**: لتصميم الواجهة (Frontend).

---

## 🚀 طريقة التشغيل | Installation & Usage

### 1. تثبيت المتطلبات (Install Requirements)
تأكد من تثبيت بايثون، ثم قم بتثبيت المكتبات اللازمة عن طريق الأمر التالي في التيرمنال:
```bash
pip install -r requirements.txt
```

### 2. تشغيل النظام (Run the App)
افتح التيرمنال داخل مجلد المشروع واكتب:
```bash
python src/app.py
```

### 3. استخدام اللوحة (Use Dashboard)
*   افتح المتصفح واذهب للرابط: `http://127.0.0.1:5000`
*   أدخل بيانات الطقس (الإشعاع والحرارة).
*   (اختياري) أدخل قراءة الطاقة الفعلية من الحساس لفحص النظام.
*   اضغط على زر **"بدء التحليل"**.

4.  **Docker (Optional)**:
    *   إنشاء نسخة : `docker build -t solar-app .`
    *   تشغيل : `docker run -p 5000:5000 solar-app`

---

## 📂 هيكلية المشروع | Project Structure
*   `Solar_Optimization_Pipeline.ipynb`: ملف جوبتر يحتوي على خطوات بناء وتدريب الموديل كاملة.
*   `src/app.py`: ملف تشغيل الموقع (السيرفر).
*   `src/templates/index.html`: ملف تصميم الواجهة.
*   `solar_model.pkl`: ملف الذكاء الاصطناعي المدرب.
*   `Dockerfile`: ملف إعدادات Docker.
*   `merged_data.csv`: البيانات التي تم استخدامها في التدريب.

---

## ⚠️ ملاحظات هامة
*   **تنبيه الغبار**: يظهر التنبيه "يوجد غبار" فقط إذا كان الفاقد في الطاقة كبير (>20%) وكان الجو مشمساً.
*   **الدقة**: الموديل يعطي نتائج دقيقة جداً لأننا استخدمنا بيانات حقيقية وتم تدريبه بعناية.

---